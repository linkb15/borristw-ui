---
title: Accordion
description: A vertically stacked set of interactive headings that each reveal a section of content.
---

import { Steps, Tabs, TabItem } from '@astrojs/starlight/components';
import { CodeBlockWrapper } from '@/components/code-block-wrapper';
import { Code } from '@astrojs/starlight/components';
import sourceCode from '@/components/ui/accordion.tsx?raw';
import previewCode from '@/components/demo/accordion-basic.tsx?raw';

import PackageManagerTabs from '@/components/package-manager-tabs.astro';
import { Basic } from '@/components/demo/accordion-basic';

A vertically stacked set of interactive headings that each reveal a section of content.

<Tabs>
  <TabItem label="Preview">
    <div className="not-content rounded-sm p-10 flex min-h-[350px] items-center justify-center">
      <Basic client:load />
    </div>
  </TabItem>
  <TabItem label="Code">
    <CodeBlockWrapper client:load>
      <Code
        class="mb-4 mt-4 max-h-[650px] min-h-32 overflow-x-auto rounded-lg relative text-sm"
        code={previewCode}
        lang="tsx"
      />
    </CodeBlockWrapper>
  </TabItem>
</Tabs>

## Installation

<Tabs>
  <TabItem label="CLI">
    <Steps>
      1.  Run the following command:

          <PackageManagerTabs>
            <Fragment slot="npm">
            ```bash frame="none"
            npx borristw-ui@latest add accordion
            ```
            </Fragment>
            <Fragment slot="pnpm">
            ```bash frame="none"
            pnpm dlx borristw-ui@latest add accordion
            ```
            </Fragment>
            <Fragment slot="yarn">
            ```bash frame="none"
            npx borristw-ui@latest add accordion
            ```
            </Fragment>
          </PackageManagerTabs>

      2.  Update `tailwind.config.js`

          Add the following animations to your `tailwind.config.js` file:

          ```diff lang="js" title="tailwind.config.js" ins={5-18}
          /** @type {import('tailwindcss').Config} */
          module.exports = {
            theme: {
              extend: {
                keyframes: {
                  'accordion-down': {
                    from: { height: '0' },
                    to: { height: 'var(--radix-accordion-content-height)' },
                  },
                  'accordion-up': {
                    from: { height: 'var(--radix-accordion-content-height)' },
                    to: { height: '0' },
                  },
                },
                animation: {
                  'accordion-down': 'accordion-down 0.2s ease-out',
                  'accordion-up': 'accordion-up 0.2s ease-out',
                },
              },
            },
          };
          ```
    </Steps>

  </TabItem>

  <TabItem label="Manual">

    <Steps>
      1.  Install the following dependencies:

          ```bash
          npm install @ark-ui/react
          ```

      2.  Copy and paste the following code into your project.

          <CodeBlockWrapper client:load>
            <Code
              class="mb-4 max-h-[650px] min-h-32 overflow-x-auto rounded-lg relative text-sm"
              code={sourceCode}
              lang="tsx"
            />
          </CodeBlockWrapper>

      3.  Update the import paths to match your project setup.

      4.  Update `tailwind.config.js`

          Add the following animations to your `tailwind.config.js` file:

          ```diff lang="js" title="tailwind.config.js" ins={5-18}
          /** @type {import('tailwindcss').Config} */
          module.exports = {
            theme: {
              extend: {
                keyframes: {
                  'accordion-down': {
                    from: { height: '0' },
                    to: { height: 'var(--radix-accordion-content-height)' },
                  },
                  'accordion-up': {
                    from: { height: 'var(--radix-accordion-content-height)' },
                    to: { height: '0' },
                  },
                },
                animation: {
                  'accordion-down': 'accordion-down 0.2s ease-out',
                  'accordion-up': 'accordion-up 0.2s ease-out',
                },
              },
            },
          };
          ```
    </Steps>

  </TabItem>
</Tabs>

## Usage

```tsx
import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from '@/components/ui/accordion';
```

```tsx
<Accordion type="single" collapsible>
  <AccordionItem value="item-1">
    <AccordionTrigger>Is it accessible?</AccordionTrigger>
    <AccordionContent>
      Yes. It adheres to the WAI-ARIA design pattern.
    </AccordionContent>
  </AccordionItem>
</Accordion>
```
